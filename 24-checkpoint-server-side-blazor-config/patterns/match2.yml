rules:
- id: untitled_rule
  pattern: |
      var builder = WebApplication.CreateBuilder(args);
      ...
      var app = builder.Build();
      ...
      app.MapRazorPages();
      ...
      app.Run();
  message: <pass>
  languages: [csharp]
  severity: INFO
